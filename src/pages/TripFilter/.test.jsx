// Did not write all the component tests
// Refer to TripHeader / .test.jsx as example

import { render, screen } from '@testing-library/react'
import NavigationButton from '.'

test('Test', () => {
  // to complete
})
